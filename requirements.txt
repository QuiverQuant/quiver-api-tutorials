# Core data + backtesting
pandas>=2.0
requests>=2.28
vectorbt>=0.26

numpy>=1.24

# Reads API keys from a .env file at the repo root (provides the `environ` module)
django-environ>=0.11

# Charting (plot_round writes both interactive HTML and static PNGs).
# Static PNG export needs the legacy kaleido, and kaleido 0.2.1 only works with
# plotly 5.x -- plotly 6 requires kaleido 1.x. Pin them together.
plotly>=5.18,<6.0
kaleido==0.2.1
